{
  "private": true,
  "devDependencies": {
    "babel-preset-jsy": "^0.9.2",
    "babel-cli": "^6.26.0",
    "nodemon": "*",
    "rollup": "^0.50.0",
    "rollup-plugin-babel": "^3.0.2"
  },
  "babel": {
    "presets": [
      "jsy/lean"
    ]
  },
  "scripts": {
    "start": "node dist/jsy_regexp.js",
    "start:dev": "nodemon -d 1 dist/jsy_regexp.js",
    "build": "rollup --config",
    "watch": "npm -s run build -- --watch",
    "demo": "babel-node test/demo.jsy",
    "pretest": "npm -s run build",
    "test": "true"
  }
}
